# Batch File Renamer - Requirements

# No external dependencies required!
# tkinter is built-in with Python 3.x on most systems

# For reference, minimum Python version:
# Python >= 3.10

# Built-in modules used:
# - tkinter (GUI)
# - pathlib (file operations)
# - threading (async rename execution)
# - datetime (timestamps)
